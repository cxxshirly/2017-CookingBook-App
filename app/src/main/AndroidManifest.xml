<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xu.chenfinalproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".AppertzerActivity" />
        <activity android:name=".Ramen1Activity" />

        <service android:name=".service.MyService" />

        <activity android:name=".EntreeActivity" />
        <activity android:name=".SoupActivity" />
        <activity android:name=".DessertActivity" />
        <activity android:name=".ServingActivity"></activity>
    </application>

</manifest>